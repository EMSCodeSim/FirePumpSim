name: firepumpsim
description: "Fire pump operator training scenarios, formulas, calculator tools, and printable pump practice."
publish_to: "none"

version: 1.0.13+8

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  go_router: ^14.6.2
  provider: ^6.1.2
  web: ^1.0.0
  url_launcher: ^6.3.0
  in_app_review: ^2.0.9
  in_app_purchase: ^3.2.3
  shared_preferences: ^2.2.3
  printing: ^5.13.4
  pdf: ^3.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
    - assets/scenarios/
    - assets/scenarios/packs/
    - assets/printable/
    - assets/printable/previews/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/dreamflow_icon.jpg
  remove_alpha_ios: true
